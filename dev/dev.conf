file-sync-client: {
  server-url: "http://localhost:8080"
  poll-interval: 1
  server-api-path: /file-sync
  server-repo-path: /git
  repos: {
    test-repo: /tmp/file-sync-test/client-repo
  }
}

file-sync-storage: {
  base-path: /tmp/file-sync-test/server-base-path
  repos: {
    test-repo: {
      working-dir: "/tmp/file-sync-test/server-working-dir",
      http-push-enabled: true
    }
  }
}

webserver: {
  port: 8080
}

web-router-service: {
  "puppetlabs.enterprise.services.file-sync-storage.file-sync-storage-service/file-sync-storage-service": {
    api: /file-sync
    repo-servlet: /git
  }
}

global: {
  logging-config = dev/logback.xml
}
